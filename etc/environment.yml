name: pyemu
channels:
  - conda-forge
dependencies:
  # required
  - python>=3.8
  - numpy
  - pandas
  # optional
  - matplotlib
  - pyshp
  - jinja2
  # tests
  - coveralls
  - flaky
  - pytest
  - pytest-cov
  - pytest-xdist
  - nbmake
  - shapely
  - pyproj
  - flopy
  - modflow-devtools
  - scikit-learn
  - pip
